node_modules/
.env
.env.*
!.env.example
*.key
*.pem
.DS_Store
.vercel
.next/
dist/

# Agent-skill installer output (npx skills add) — tooling, not source.
# Untracked and unignored, these get swept up by any `git add .` in a parallel lane.
.agents/
.claude/
agent/
skills-lock.json

# Personal working notes kept beside the code they describe. Never source.
*.local.md

.vercel
