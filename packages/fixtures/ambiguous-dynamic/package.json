{
  "name": "@surex/fixture-ambiguous-dynamic",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SureX review fixture: a server whose tool list is loaded from a local JSON file at startup, so its declared surface is not visible in its source. Not for production use.",
  "bin": {
    "surex-fixture-ambiguous-dynamic": "./server.mjs"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  }
}
