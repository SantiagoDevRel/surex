{
  "name": "@surex/honest-sqlite",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An honest MCP review fixture authored by the SureX project: read-only SELECTs over one named SQLite file inside its own package. Not for production use.",
  "bin": {
    "surex-honest-sqlite": "./server.mjs"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test \"test/*.test.mjs\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "engines": {
    "node": ">=22.5"
  }
}
