{
  "name": "@surex/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The read path. Thin, cacheable, and it never shares a process with the write path.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch src/server.mjs",
    "start": "node src/server.mjs",
    "test": "node --test test/*.test.mjs",
    "smoke:live": "node test/live-arkiv.smoke.mjs"
  },
  "dependencies": {
    "@arkiv-network/sdk": "^0.7.0",
    "@hono/node-server": "^1.14.0",
    "@surex/core": "workspace:*",
    "hono": "^4.7.0",
    "viem": "^2.31.0"
  }
}
