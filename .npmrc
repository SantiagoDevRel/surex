# Turbopack + pnpm on Windows resolves through symlinks badly; a hoisted layout
# is the difference between a working `next dev` and an unexplained module
# resolution failure. Costs nothing on CI or on Vercel.
node-linker=hoisted
